/// Base configuration for talking to the Rust backend API.
/// (Minimal placeholder, extended in future phases.)
class ApiClientConfig {
  const ApiClientConfig({required this.baseUrl});

  final String baseUrl;
}
